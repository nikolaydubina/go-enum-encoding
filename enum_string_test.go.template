// Code generated by go-enum-encoding; DO NOT EDIT.

package {{.Package}}

import (
	"testing"
)

func Test{{.Type}}_{{.StringMethod}}(t *testing.T) {
	values := []{{.Type}}{{{.Values}}}
	tags := []string{{{.Tags}}} 

	for i := range values {
		if values[i].{{.StringMethod}}() != tags[i] {
			t.Errorf("got(%s) != exp(%s)", values[i].{{.StringMethod}}() , tags[i]) 
		}	
	}
}

func Benchmark{{.Type}}_{{.StringMethod}}(b *testing.B) {
	var v string
	for i := 0; i < b.N; i++ {
		for _, c := range []{{.Type}}{{{.Values}}} {
			v = c.{{.StringMethod}}()
		}
	}
	if len(v) > 1000 {
		b.Fatal("noop")
	}
}
