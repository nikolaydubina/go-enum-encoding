// Code generated by go-enum-encoding; DO NOT EDIT.

package {{.Package}}

import (
	"encoding/json"
	"errors"
	"fmt"
	"slices"
	"testing"
	"math/rand"
)

func Example{{.Type}}_MarshalText() {
	for _, v := range []{{.Type}}{{{.Values}}} {
		b, _ := v.MarshalText()
		fmt.Printf("%s ", string(b))
	}
	// Output: {{.TagsNaked}}
}

func Example{{.Type}}_UnmarshalText() {
	for _, s := range []string{{{.Tags}}} {
		var v {{.Type}}
		if err := (&v).UnmarshalText([]byte(s)); err != nil {
			fmt.Println(err)
		}
	}
}

func Test{{.Type}}_MarshalText_UnmarshalText(t *testing.T) {
	for _, v := range []{{.Type}}{{{.Values}}} {
		b, err := v.MarshalText()
		if err != nil {
			t.Errorf("cannot encode: %s", err)
		}

		var d {{.Type}}
		if err := (&d).UnmarshalText(b); err != nil {
			t.Errorf("cannot decode: %s", err)
		}

		if d != v {
			t.Errorf("exp(%v) != got(%v)", v, d)
		}
	}

	t.Run("when unknown value, then error", func(t *testing.T) {
		s := `something`
		var v {{.Type}}
		err := (&v).UnmarshalText([]byte(s))
		if err == nil {
			t.Error("must be error")
		}
		if !errors.Is(err, ErrUnknown{{.Type}}) {
			t.Error("wrong error", err)
		}
	})
}

func Test{{.Type}}_JSON(t *testing.T) {
	type V struct {
		Values []{{.Type}} `json:"values"`
	}

	values := []{{.Type}}{{{.Values}}}

	var v V
	s := `{"values":[{{.Tags}}]}`
	json.Unmarshal([]byte(s), &v)

	if len(v.Values) != len(values) {
		t.Errorf("cannot decode: %d", len(v.Values))
	}
	if !slices.Equal(v.Values, values) {
		t.Errorf("wrong decoded: %v", v.Values)
	}

	b, err := json.Marshal(v)
	if err != nil {
		t.Fatalf("cannot encode: %s", err)
	}
	if string(b) != s {
		t.Errorf("wrong encoded: %s != %s", string(b), s)
	}

	t.Run("when unknown value, then error", func(t *testing.T) {
		s := `{"values":["something"]}`
		var v V
		err := json.Unmarshal([]byte(s), &v)
		if err == nil {
			t.Error("must be error")
		}
		if !errors.Is(err, ErrUnknown{{.Type}}) {
			t.Error("wrong error", err)
		}
	})
}


func Benchmark{{.Type}}_UnmarshalText(b *testing.B) {
	vb := seq_bytes_{{.Type}}[rand.Intn(len(seq_bytes_{{.Type}}))]

	var x {{.Type}}

	for b.Loop() {
		_ = x.UnmarshalText(vb)
	}
}

func Benchmark{{.Type}}_AppendText(b *testing.B) {
	bb := make([]byte, 10)
	
	vs := []{{.Type}}{{{.Values}}} 
	v := vs[rand.Intn(len(vs))]

	for b.Loop() {
		_, _ = v.AppendText(bb)
	}
}

func Benchmark{{.Type}}_MarshalText(b *testing.B) {
	vs := []{{.Type}}{{{.Values}}} 
	v := vs[rand.Intn(len(vs))]

	for b.Loop() {
		_, _ = v.MarshalText()
	}
}
